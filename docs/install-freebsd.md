# Installing solar-monitor on FreeBSD

FreeBSD is the reference platform for solar-monitor.

This document describes the basic development and installation process.

## Requirements

Install the RTL-SDR tools:

```sh
pkg install rtl-sdr
